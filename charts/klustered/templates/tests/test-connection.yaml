apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "klustered.fullname" . }}-test-connection"
  labels:
    {{- include "klustered.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "klustered.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
